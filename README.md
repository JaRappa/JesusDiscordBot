# ✝️ Jesus Discord Bot

A Discord bot that embodies the love and kindness of Jesus, gently reminding server members to be kind when they use harsh language or send mean messages.

## Features

- 📝 **Curse Word Detection** - Detects curse words from a customizable list
- 🧠 **AI Sentiment Analysis** - Uses OpenAI GPT-5 Nano to detect mean/unkind messages
- 💬 **AI-Generated Responses** - Jesus-like responses generated by AI for variety
- ⏱️ **Cooldown System** - Prevents spam by limiting responses per user
- 🎚️ **Configurable** - Adjust response chance, thresholds, and more

## Setup Instructions

### 1. Install Node.js

Download and install Node.js (v18 or later) from: https://nodejs.org/

### 2. Create a Discord Bot

1. Go to [Discord Developer Portal](https://discord.com/developers/applications)
2. Click "New Application" and give it a name (e.g., "Jesus Bot")
3. Go to the "Bot" section in the left sidebar
4. Click "Add Bot"
5. Under "Privileged Gateway Intents", enable:
   - ✅ **MESSAGE CONTENT INTENT** (Required!)
   - ✅ **SERVER MEMBERS INTENT**
6. Click "Reset Token" and copy your bot token (keep it secret!)

### 3. Invite the Bot to Your Server

1. In the Developer Portal, go to "OAuth2" > "URL Generator"
2. Select scopes: `bot`
3. Select bot permissions:
   - Read Messages/View Channels
   - Send Messages
   - Read Message History
4. Copy the generated URL and open it in your browser
5. Select your server and authorize

### 4. Get OpenAI API Key

1. Go to [OpenAI Platform](https://platform.openai.com/api-keys)
2. Create an account or sign in
3. Generate a new API key
4. Add some credits to your account (GPT-5 Nano is very cheap!)

### 5. Configure the Bot

1. Copy `.env.example` to `.env`:
   ```
   copy .env.example .env
   ```

2. Edit `.env` and add your tokens:
   ```
   DISCORD_TOKEN=your_discord_bot_token_here
   OPENAI_API_KEY=your_openai_api_key_here
   ```

### 6. Install Dependencies

Open a terminal in this folder and run:
```
npm install
```

### 7. Run the Bot

```
npm start
```

You should see:
```
═══════════════════════════════════════════
✝️  Jesus Bot is now online!
📛 Logged in as: YourBot#1234
🏠 Serving 1 server(s)
═══════════════════════════════════════════
```

## Running 24/7 on Windows

### Option 1: Task Scheduler (Recommended)

1. Open Task Scheduler
2. Create a new task
3. Set trigger to "At startup"
4. Set action to run: `node bot.js` in this directory
5. Enable "Run whether user is logged on or not"

### Option 2: PM2 (Process Manager)

```bash
npm install -g pm2
pm2 start bot.js --name jesus-bot
pm2 startup
pm2 save
```

### Option 3: Run in Background

Create a `start-bot.bat` file:
```batch
@echo off
cd /d "C:\Users\Jake\Documents\JesusDiscordBot"
node bot.js
```

## Customization

### Adding/Removing Curse Words

Edit `curseWords.js` to add or remove words from the detection list.

### Adjusting Sensitivity

In `bot.js`, you can modify:
- `SENTIMENT_THRESHOLD` - How confident the AI must be (0.0-1.0)
- `COOLDOWN_MS` - Cooldown time between responses per user
- `RESPONSE_CHANCE` - Probability of responding (1.0 = always)

### Customizing Jesus's Personality

Edit the system prompt in `openaiService.js` under `generateJesusResponse()` to adjust how Jesus speaks.

## Troubleshooting

**Bot is online but not responding:**
- Make sure MESSAGE CONTENT INTENT is enabled in Discord Developer Portal
- Check that the bot has permission to read and send messages in the channel

**"DISCORD_TOKEN not found" error:**
- Make sure you created a `.env` file (not `.env.example`)
- Make sure there are no spaces around the `=` sign

**OpenAI errors:**
- Check that your API key is correct
- Make sure you have credits in your OpenAI account
- If gpt-5-nano isn't available, change MODEL in `openaiService.js` to `gpt-4o-mini`

## 🙏 Peace be with you!
